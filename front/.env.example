# Front (Vite). Copiar a `.env` si el API no es el default.
# Vite solo inyecta variables con prefijo VITE_.

# Base del Nest (sin barra final). Health: ${VITE_API_BASE_URL}/health/db
# API versionada: ${VITE_API_BASE_URL}/api/v1/...
VITE_API_BASE_URL=http://localhost:3001
